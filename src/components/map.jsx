import React from 'react';

// This will show the map of surroundings and show character moving around
// For now, this is just a placeholder

class Map extends React.Component {
  constructor (props) {
    super (props);
    this.state = {
      //
    }
  }

  render () {
    return (
      <div>
        Map
      </div>
    )
  }
}

export default Map;
